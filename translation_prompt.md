# Wizardry 7 Gold 한국어 번역 작업 프롬프트

아래 Google 스프레드시트에서 `Messages`와 `Scenario` 탭의 영어 원문을 한국어로 번역해 주세요.

Google Sheets: 이 프롬프트와 함께 제공된 "Wizardry 7 Gold 한국어 번역 데이터" 시트

## 작업 범위

- `source_text`가 비어 있지 않고 `translation`이 비어 있는 행만 번역합니다.
- 번역 결과는 같은 행의 `translation` 열에만 입력합니다.
- ID, 오프셋, 길이, 분류, 원문 및 다른 열은 절대 수정하지 않습니다.
- 한 번에 200행 이하로 처리하고, 매 배치가 끝날 때 저장 상태를 확인합니다.
- 이미 번역된 셀은 덮어쓰지 않습니다.

## 번역 문체

- 1990년대 정통 판타지 CRPG 분위기의 자연스러운 한국어를 사용합니다.
- 설명문과 대사는 의미를 정확히 살리되 화면 폭을 고려해 간결하게 번역합니다.
- UI 명령은 짧고 명확한 명령형 또는 명사형으로 통일합니다.
- 인명·지명·종족·직업·아이템·주문 이름은 `Glossary` 탭을 우선 적용합니다.
- 새 고유명사가 나오면 기존 번역을 검색한 후 같은 표기를 사용하고, 필요하면 `Glossary`에 추가합니다.
- 원문에 오탈자나 어색한 문장이 있어도 의미를 추론해 자연스럽게 번역하되 `notes`에 짧게 기록합니다.

## 절대 보존할 요소

- `<0x01>` 같은 `<0xNN>` 제어코드는 개수와 순서를 그대로 유지합니다.
- `$`, `^`, `%`, `@`, `#`, `*`, `/` 등 원문에 있는 특수기호와 자리표시자는 임의로 삭제하거나 재배열하지 않습니다.
- 숫자 자리표시자, 캐릭터명 삽입 위치, 문단 구분 표식은 그대로 둡니다.
- 제어코드나 자리표시자의 의미가 불확실하면 번역하지 말고 원형을 보존한 뒤 `notes`에 표시합니다.

## 탭별 규칙

### Messages

- 긴 문장은 뜻을 유지하면서 가능한 한 간결하게 번역합니다.
- `$`가 포함된 경우 원문의 위치를 그대로 유지합니다. 실제 게임에서 줄바꿈 또는 치환 표식일 수 있습니다.
- 한 레코드의 번역은 가능한 한 255바이트 이내가 되도록 짧게 작성합니다. 최종 바이트 길이는 별도 삽입 도구에서 검증합니다.

### Scenario

- 아이템명과 몬스터명은 짧고 게임 용어답게 번역합니다.
- `capacity`는 현재 16바이트 고정 슬롯을 뜻합니다.
- 한글을 2바이트 코드로 넣을 가능성을 고려해 가능한 한 7~8음절 이하로 작성합니다.
- 원문에 `/`가 있으면 줄 구분 표식일 수 있으므로 같은 개수와 순서를 보존합니다.
- `singular`, `plural`, `generic_singular`, `generic_plural` 변형은 한국어에서 동일하게 번역해도 되지만 의미 차이가 있으면 구분합니다.

## 품질 확인

각 배치가 끝날 때 다음을 검사합니다.

1. 원문이 있는 모든 대상 행에 번역이 들어갔는지 확인
2. `<0xNN>` 토큰의 개수와 순서가 원문과 같은지 확인
3. `$`, `^`, `%`, `@`, `/` 등 특수기호가 보존됐는지 확인
4. 동일 고유명사가 같은 한국어 표기로 번역됐는지 확인
5. 지나치게 긴 번역은 더 간결하게 다듬기

작업을 시작할 때 먼저 `Instructions`와 `Glossary` 탭을 읽고, 아직 번역되지 않은 첫 행부터 진행하세요.
